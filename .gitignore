node_modules/
.wrangler/
.dev.vars
.seed.sql
dist/
*.log
scripts/secrets.bat

# local logical backups (scripts/backup.mjs)
backups/

# mcp-publisher binary and its signing key (never commit the key)
.tools/
key.pem
mcp-registry-auth

# Screen recordings for the Slop Trawl videos: never commit these
*.mp4
*.mkv
*.mov
